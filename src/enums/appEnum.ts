// 系统级别枚举

// 菜单类型
export enum MenuTypeEnum {
  LEFT = 'left', // 左侧菜单
  TOP = 'top', // 顶部菜单
  TOP_LEFT = 'top-left', // 顶部左侧菜单
  DUAL_MENU = 'dual-menu' // 双菜单
}

// 系统主题
export enum SystemThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
  AUTO = 'auto'
}

// 菜单主题
export enum MenuThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
  DESIGN = 'design'
}

// 菜单宽度
export enum MenuWidth {
  CLOSE = '70px'
}

// 语言
export enum LanguageEnum {
  ZH = 'zh'
}

// 图标类型
export enum IconTypeEnum {
  CLASS_NAME = 'className',
  UNICODE = 'unicode'
}

// 容器宽度
export enum ContainerWidthEnum {
  FULL = '100%',
  BOXED = '1200px'
}

// 背景颜色枚举
export enum BgColorEnum {
  PRIMARY = 'bg-primary',
  SECONDARY = 'bg-secondary',
  WARNING = 'bg-warning',
  ERROR = 'bg-error',
  SUCCESS = 'bg-success',
  DANGER = 'bg-danger',
  INFO = 'bg-info'
}
